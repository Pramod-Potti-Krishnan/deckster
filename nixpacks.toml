[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
    "python -m pip install -r requirements-prod.txt || python -m pip install -r requirements.txt"
]

[start]
cmd = "python -m uvicorn src.api.main:app --host 0.0.0.0 --port $PORT --workers 1"

[variables]
PYTHONPATH = "/app"